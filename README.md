# Habit Tracker API

## Описание

RESTful API для управления полезными привычками и целями с полным набором CRUD-операций и статистикой.

**Основные возможности:**
- 📝 Управление привычками (создание, просмотр, обновление, удаление)
- 🎯 Управление целями
- ✅ Отслеживание выполнения привычек
- 📊 Получение подробной статистики

> 💡 Все эндпоинты работают по адресу: `http://localhost:3000/api/v1/...`  
> Все тела запросов и ответов в формате **JSON** с заголовком `Content-Type: application/json`.

---

## Эндпоинты

### Привычки (`/api/v1/habits`)

| Действие                 | Метод    | URL                           | Описание                |
| ------------------------ | -------- | ----------------------------- | ----------------------- |
| Получить все             | `GET`    | `/api/v1/habits`              | Список всех привычек    |
| Получить по ID           | `GET`    | `/api/v1/habits/:id`          | Детали привычки         |
| Создать                  | `POST`   | `/api/v1/habits`              | Добавить новую привычку |
| Обновить                 | `PUT`    | `/api/v1/habits/:id`          | Изменить привычку       |
| Удалить                  | `DELETE` | `/api/v1/habits/:id`          | Удалить привычку        |
| Отметить как выполненную | `PUT`    | `/api/v1/habits/:id/complete` | —                       |

### Цели (`/api/v1/goals`)

| Действие                 | Метод    | URL                          | Описание            |
| ------------------------ | -------- | ---------------------------- | ------------------- |
| Получить все             | `GET`    | `/api/v1/goals`              | Список всех целей   |
| Получить по ID           | `GET`    | `/api/v1/goals/:id`          | Детали цели         |
| Создать                  | `POST`   | `/api/v1/goals`              | Добавить новую цель |
| Обновить                 | `PUT`    | `/api/v1/goals/:id`          | Изменить цель       |
| Удалить                  | `DELETE` | `/api/v1/goals/:id`          | Удалить цель        |
| Отметить как выполненную | `PUT`    | `/api/v1/goals/:id/complete` | —                   |

### Отслеживания (`/api/v1/tracks`)

| Действие       | Метод    | URL                  | Описание            |
| -------------- | -------- | -------------------- | ------------------- |
| Получить все   | `GET`    | `/api/v1/tracks`     | Список всех записей |
| Получить по ID | `GET`    | `/api/v1/tracks/:id` | Детали записи       |
| Создать        | `POST`   | `/api/v1/tracks`     | Добавить запись     |
| Обновить       | `PUT`    | `/api/v1/tracks/:id` | Изменить запись     |
| Удалить        | `DELETE` | `/api/v1/tracks/:id` | Удалить запись      |

### Статистика

| Действие            | Метод | URL                  | Описание                                |
| ------------------- | ----- | -------------------- | --------------------------------------- |
| Получить статистику | `GET` | `/api/v1/statistics` | Сводная статистика по привычкам и целям |

---

## Как добавить привычку или цель (Postman)

### 📌 Чтобы добавить **привычку**, пользователь должен отправить:

- **Method:** `POST`
- **URL:** `http://localhost:3000/api/v1/habits`
- **Headers:**
  - `Content-Type: application/json`
- **Body (raw → JSON):**
  ```json
  {
    "name": "Пить воду",
    "description": "2 литра в день",
    "category": "здоровье",
    "frequency": "ежедневно"
  }
  🎯 Чтобы добавить цель, пользователь должен отправить:
Method: POST
URL: http://localhost:3000/api/v1/goals
Headers:
Content-Type: application/json
Body (raw → JSON):
{
  "title": "Пробежать марафон",
  "description": "Подготовка за 6 месяцев",
  "targetDate": "2026-06-01",
  "category": "спорт"
}
Что API принимает: title (обязательно), description, targetDate, category
Что возвращает (201): объект цели с id и createdAt
Ошибка (400): если не передано title

Детали всех эндпоинтов
GET /api/v1/habits
Возвращает: массив привычек
Код: 200
GET /api/v1/habits/:id
Принимает: id в URL
Возвращает: одну привычку
Коды: 200 — найдена, 404 — не найдена
POST /api/v1/habits
Принимает: JSON с полями (см. выше)
Возвращает: созданную привычку
Коды: 201 — успех, 400 — ошибка валидации
PUT /api/v1/habits/:id
Принимает: id в URL + частичное обновление в теле
Возвращает: обновлённую привычку
Коды: 200 — успех, 404 — не найдена
DELETE /api/v1/habits/:id
Принимает: id в URL
Возвращает: ничего
Код: 204
PUT /api/v1/habits/:id/complete
Принимает: id в URL
Возвращает: сообщение и дату выполнения
Код: 200
POST /api/v1/goals
Принимает: JSON с title, description, targetDate, category
Возвращает: созданную цель
Коды: 201 — успех, 400 — ошибка
PUT /api/v1/goals/:id/complete
Принимает: id в URL
Возвращает: сообщение и дату завершения
Код: 200
POST /api/v1/tracks
Принимает: habitId (обязательно), date, notes
Пример:
{
  "habitId": 1,
  "date": "2025-12-12",
  "notes": "Выполнил легко"
}
Возвращает: запись отслеживания
Коды: 201 — успех, 400 — ошибка
GET /api/v1/statistics
Возвращает: объект со статистикой по привычкам и целям
Код: 200
Пример ответа:
{
  "habits": { "total": 5, "completed": 3, "completionRate": 60 },
  "goals": { "total": 2, "completed": 1, "completionRate": 50 },
  "streak": { "current": 7, "longest": 12 },
  "dailyCompletion": [
    { "date": "2025-12-12", "completed": 4, "total": 5 }
  ]
}

Примеры использования
# Получить все привычки
curl http://localhost:3000/api/v1/habits

# Создать привычку
curl -X POST http://localhost:3000/api/v1/habits \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Пить воду",
    "description": "2 литра в день",
    "category": "здоровье",
    "frequency": "ежедневно"
  }'

# Отметить привычку как выполненную (ID = 5)
curl -X PUT http://localhost:3000/api/v1/habits/5/complete

# Получить статистику
curl http://localhost:3000/api/v1/statistics
